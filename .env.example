# Orkestra Ventures - Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================
# DATABASE CONFIGURATION (Required)
# ============================================

# Railway MySQL Connection String
# Get this from: Railway Dashboard → MySQL Service → Variables → MYSQL_URL
# Format: mysql://root:PASSWORD@HOST:PORT/railway
# Example: mysql://root:abc123xyz@containers-us-west-123.railway.app:7654/railway
DATABASE_URL="mysql://root:PASSWORD@HOST:PORT/railway"

# ============================================
# SESSION CONFIGURATION (Required)
# ============================================

# Secret key for session encryption
# Use a random 32+ character string
# Generate at: https://randomkeygen.com
# Example: my-super-secret-session-key-12345678
SESSION_SECRET="your-random-secret-key-here"

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Environment mode (development, production, test)
# Vercel sets this automatically to "production"
NODE_ENV="production"

# Local development port
# Only used when running locally with "pnpm dev"
PORT=3000

# ============================================
# RAILWAY SETUP INSTRUCTIONS
# ============================================

# 1. Go to https://railway.app
# 2. Login with GitHub (free $5 credit)
# 3. Click "New Project" → "Provision MySQL"
# 4. Click on MySQL service → "Variables" tab
# 5. Copy the "MYSQL_URL" value
# 6. Paste it as DATABASE_URL above

# ============================================
# VERCEL DEPLOYMENT
# ============================================

# To deploy to Vercel:
# 1. Add DATABASE_URL and SESSION_SECRET to Vercel environment variables
# 2. Go to: Vercel Dashboard → Settings → Environment Variables
# 3. Add both variables for Production, Preview, and Development
# 4. Redeploy your project

# To pull environment variables locally:
# vercel env pull .env.local

# ============================================
# SECURITY NOTES
# ============================================

# ⚠️ NEVER commit .env or .env.local to Git
# ⚠️ Keep DATABASE_URL secret (contains password)
# ⚠️ Use strong SESSION_SECRET (32+ characters)
# ✅ Add .env.local to .gitignore (already done)
# ✅ Use different secrets for dev/staging/production
